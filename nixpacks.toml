# Railway build - prisma/schema.prisma in root satisfies npx prisma generate
[variables]
# Dummy URL for prisma generate during build (no DB connection needed)
DATABASE_URL = "postgresql://localhost:5432/dummy"
